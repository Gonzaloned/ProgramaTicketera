    def createConnection(self):
        
        DRIVER='ODBC Driver 17 for SQL Server'
        SERVER_NAME = 'GONZALO\DBGON'
        DATABASE_NAME = 'turnos'
        USERNAME = 'gon'
        PASSWORD = '123456'

        global con
        print('entre')

        con = QSqlDatabase().addDatabase('QODBC')#Set the new database driver (in this case QODBC)
     
        #Setting attributes string to start connection
        conn_str = (   
        r'DRIVER={SQL Server};'
        r'SERVER=GONZALO\DBGON;'
        r'DATABASE=turnos;'
        r'Trusted_Connection=yes;'
        )

        #Set the db Server DRIVER,SERVER,DATABASE
        con.setDatabaseName(conn_str)

        print('starting connection')
        #Check connection
        if con.open():
            print(' Succesfull connection')
        else:
            print("Database Error: %s"  % con.lastError().databaseText())
            exit(0)