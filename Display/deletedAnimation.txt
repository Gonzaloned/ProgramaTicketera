    def refreshDisplay2(self, num: int, caja: int):
        for pos in range(5): #range(Start, End, Step)
            #if pos==0:
            #    self.animateOff(self.listaEffNum[pos])  #Fade actual label
            #    QTimer.singleShot(1000, lambda: self.animateOn(self.listaEffNum[pos],pos)) #Start timer to show new number slowly
            print(f'entre al for, posicion {pos} ')
            self.animateOff(self.listaEffNum[pos])  #Fade actual label
            QTimer.singleShot(1000, lambda: self.animateOn(self.listaEffNum[pos])) #Start timer to show new number slowly
            #self.listaNum[actualpos].setText(self.listaNum[actualpos-1].text)    #Change actual label to the pos-1 label

    def refreshDisplay(self, num: int, caja: int, pos: int):
        if pos==0:
            QTimer.singleShot(1000, lambda: self.animateOn(self.animateOff(self.listaEffNum[pos])))
            #self.animateOff(self.listaEffNum[pos])
              #Fade actual label
            QTimer.singleShot(1000, lambda: self.animateOn(self.listaEffNum[pos]))
        else:            
            self.refreshDisplay(num,caja,pos-1)
            QTimer.singleShot(1000, lambda: self.animateOn(self.animateOff(self.listaEffNum[pos])))
            QTimer.singleShot(1000, lambda: self.animateOn(self.listaEffNum[pos]))
